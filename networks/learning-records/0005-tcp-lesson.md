# TCP 课程已发布

用户完成 IP 网络层后，按自然路径向上进入传输层。已发布第 3 课（TCP 深度解析）和 TCP 参考卡。课程覆盖：三次握手的设计动机、序列号/ACK 机制、滑动窗口流量控制、拥塞控制的完整状态机（慢启动→拥塞避免→快速恢复）、四次挥手与 TIME_WAIT、TCP vs UDP 适用场景。

**Implications**：TCP 是面试最高频考点，后续可围绕 QUIC/HTTP3、BBR 拥塞控制、SYN Flood 防御等高级话题展开。用户已完成链路层→网络层→传输层的自底向上路径，对"逐跳转发"和"端到端可靠"的对照已有清晰认知。
